exports.config = {
	// Default port to serve on (can be overridden with param at startup)
	PORT: 3000,
	// Local url for testing.
	TEST_URL: 'http://localhost:8080/data.xml',
	// URL to airport XML file.
	URL: 'https://www.syrairport.org/flightdata/data.xml'
}
